global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # OpenLIT GenAI metrics, forwarded by the OTel collector.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
